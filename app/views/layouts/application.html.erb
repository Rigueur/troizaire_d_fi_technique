<!DOCTYPE html>
<html>
  <head>
    <title>TroizaireDFiTechnique</title>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <%= yield %>
    <%= render "shared/flashes" %>
    <% unless current_page?(root_path) %>
      <button class="btn btn-dark", style= "position: fixed; bottom: 20px; right: 20px; z-index: 100;"><%= link_to "Home", root_path, class: "text-decoration-none text-white" %></button>
    <% end %>
  </body>
</html>
